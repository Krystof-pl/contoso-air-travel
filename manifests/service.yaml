apiVersion: v1
kind: Service
metadata:
  name: air-travel
  namespace: dev
  labels:
    app.kubernetes.io/name: air-travel
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: air-travel
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000